# Test and lint dependencies. The suite is offline: it makes no network calls and
# needs no API keys, so it runs anywhere the runtime dependencies install.
-r requirements.txt

pytest>=8.0
ruff>=0.5

# starlette's TestClient is built on httpx; without it any test that exercises
# the FastAPI routes cannot construct a client. Nothing in requirements.txt
# pulls it in, so it is pinned here rather than left to chance.
httpx>=0.27
